import React, { useEffect, useState } from 'react';

const Like = (posts) => {

    const [liked, setLiked] =useState(false)
    useEffect(()=>{
        // if() setLiked(true)
    })

    return (
        <div>
            
        </div>
    );
};

export default Like;